[project]
name = "lategrep-docs"
version = "0.1.0"
description = "Documentation and comparison tests for lategrep"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.20.0",
    "fast-plaid>=0.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]
